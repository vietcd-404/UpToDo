<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/txt_forgot_password_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp"
        android:text="Quên mật khẩu"
        android:textSize="28sp"
        android:textStyle="bold" />

    <EditText
        android:id="@+id/edtTenTK"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_below="@id/txt_forgot_password_title"
        android:inputType="text"
        android:layout_marginTop="32dp"
        android:hint="Nhập tên tài khoản" />

    <EditText
        android:id="@+id/edt_email_forgot_password"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_below="@id/edtTenTK"
        android:layout_marginTop="32dp"
        android:hint="Nhập email đăng ký tài khoản"
        android:inputType="textEmailAddress" />

    <Button
        android:id="@+id/btn_send_email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/edt_email_forgot_password"
        android:layout_marginTop="32dp"
        android:backgroundTint="#7F5AF0"
        android:text="Gửi email"
        android:textColor="#FFFFFF" />

</RelativeLayout>
